{
  "rules": {
    ".read": "auth != null || auth == null",
    ".write": "auth != null",
    "status": {
      ".indexOn": [
        "postedDate",
        "joinedNumber",
        "city"
      ]
    }
  }
}
